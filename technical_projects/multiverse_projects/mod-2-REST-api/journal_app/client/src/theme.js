import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#001858",
    },
    secondary: {
      main: "#8bd3dd",
    },
    tertiary: {
      main: "#001858",
    },
    background: {
      main: "#fef6e4",
    },
  },
});

export default theme;
